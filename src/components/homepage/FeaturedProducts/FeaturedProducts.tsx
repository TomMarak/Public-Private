import { ProductGrid } from '@/components/product';

export default function FeaturedProducts() {
  return (
    <section className="featured-products">
      <h2>Doporučované produkty</h2>
      {/* TODO: Fetch featured products from Payload */}
    </section>
  );
}
